import React from 'react';
import ProfileCard from './ProfileCard'; // Adjust the path based on your folder structure

const App = () => {
  return (
    <div className="min-h-screen bg-gray-900 flex justify-center items-center">
      <ProfileCard />
    </div>
  );
};

export default App;
